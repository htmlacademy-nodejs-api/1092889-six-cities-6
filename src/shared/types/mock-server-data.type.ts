import {Location} from './location.type.js';

type MockServerData = {
  titles: string[],
  descriptions: string[],
  cities: string[],
  previewImages: string[],
  offerImages: string[],
  types: string[],
  goods: string[],
  userNames: string[]
  locations: Location[],
  emails: string[],
  avatars: string[],
  userTypes: string[]
}

export {MockServerData};
